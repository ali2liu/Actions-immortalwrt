diff --git a/Makefile b/Makefile
index ... .. ...
--- a/Makefile
+++ b/Makefile
@@ -10,7 +10,7 @@ include ../../luci.mk
 
 define Package/luci-app-openvpn-server
   $(call Package/luci/webtemplate)
-  TITLE:=LuCI support for OpenVPN Server
+  TITLE:=LuCI support for OpenVPN Server (custom renamed config)
   DEPENDS:=+luci-compat +openvpn-openssl +openvpn-easy-rsa +kmod-tun
 endef
 
@@ -18,7 +18,7 @@ define Package/luci-app-openvpn-server/conffiles
-/etc/config/openvpn
+/etc/config/openvpn-server
 endef
 
 define Package/luci-app-openvpn-server/install
@@ -26,8 +26,8 @@ define Package/luci-app-openvpn-server/install
 	$(INSTALL_DIR) $(1)/etc/config
 	$(INSTALL_CONF) ./files/openvpn-server.config $(1)/etc/config/openvpn-server   # 如果没有这个文件，就用下面的空文件或跳过
 
-	$(INSTALL_DIR) $(1)/etc/init.d
-	$(INSTALL_BIN) ./files/openvpn.init $(1)/etc/init.d/openvpn   # 原版可能没有或不同，改成下面
+	$(INSTALL_DIR) $(1)/etc/init.d
+	$(INSTALL_BIN) ./files/openvpn-server.init $(1)/etc/init.d/openvpn-server
 endef
 
 $(eval $(call BuildPackage,luci-app-openvpn-server))
